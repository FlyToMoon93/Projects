"use client";

import Layout from "@/app/layout/Layout";

const DeleteAccount: React.FC = () => {

    return (
        <Layout>
          test
        </Layout>
    );
};

export default DeleteAccount;
